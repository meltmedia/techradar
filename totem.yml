variables:
  default_private_host_suffix: ''
  check_path: '/'
  private_host:
    value: |
      # if ref == 'master'
      techradar.au.melt.sh,techradar.meltmedia.com
      # elif ref == 'develop'
      techradar.cu.melt.sh
      # endif
  private_port: 8080
