variables:
  check_path: '/'
  default_private_host_suffix: ''
  docker_command: 'start'
  private_host:
    value: |
      # if ref == 'master'
      techradar.au.melt.sh
      # elif ref == 'develop'
      techradar.cu.melt.sh
      # endif
  private_port: 8080
